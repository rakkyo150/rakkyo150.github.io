{
  "scripts": {
    "wrangler": "wrangler",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"local": "hugo --gc --minify && wrangler dev"
	},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251011.0",
    "wrangler": "^4.43.0"
  }
}
